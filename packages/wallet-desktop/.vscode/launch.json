{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug i3M-Wallet desktop",
      "type": "node",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "env": { "DEBUG": "*" },
      "runtimeExecutable": "${workspaceFolder}/node_modules/.bin/electron",
      "args" : ["--no-sandbox", "."],
      "outputCapture": "std"
    }
  ]
}
