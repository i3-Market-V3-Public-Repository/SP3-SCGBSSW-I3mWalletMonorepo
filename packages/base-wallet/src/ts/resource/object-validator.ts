import { ObjectResource } from '../app'
import { Validator } from './resource-validator'

export const objectValidator: Validator<ObjectResource> = async (resource, veramo) => {
  const errors: Error[] = []

  return errors
}
